# This is a comment! A comment allows you to write within the coding environemnt without causing any issues.
# It serves as a gentle reminder of what the codes are doing and so forth so that the programmer will not forget.
# If you are using Visual Studio Code, the hotkey is "ctrl + /". You can do it manually by just putting the hash(#) symbol at the beginning of the line.